#!/usr/bin/env bash
# shellcheck shell=bash
# #############################################################################
# SPDX-FileComment: Download and install package kz
#
# SPDX-FileCopyrightText: Karel Zimmer <info@karelzimmer.nl>
# SPDX-License-Identifier: CC0-1.0
# #############################################################################


# #############################################################################
# Imports
# #############################################################################

export TEXTDOMAIN='kz'
export TEXTDOMAINDIR='/usr/share/locale'
source /usr/bin/gettext.sh


# #############################################################################
# Constants
# #############################################################################

# List NORMAL last here so that -x doesn't bork the display.
readonly RED='\033[1;31m'
readonly NORMAL='\033[0m'


# #############################################################################
# Globals
# #############################################################################

declare OPTION_DEV=false


# #############################################################################
# Functions
# #############################################################################

# This function performs initial actions.
function init() {
    if [[ $* = -* && '--dev' == *"$*"* ]]; then
        OPTION_DEV=true
    elif [[ -n $* ]]; then
        printf '%s\n' 'kz get [-d | --dev]'
        exit 0
    fi

    # Check if systemd is available.
    if ! type systemctl > /dev/null; then
        printf "$RED%s$NORMAL\n" 'fatal: no systemd available' >&2
        exit 1
    fi

    # Check if os release is available.
    if ! [[ -f /etc/os-release ]]; then
        printf "$RED%s$NORMAL\n" 'fatal: no os release available' >&2
        exit 1
    fi

    # Check if started as root.
    if [[ $UID -eq 0 ]]; then
        printf "$RED%s$NORMAL\n" 'fatal: must not be run as root' >&2
        exit 1
    fi
}

# This function performs the script actions.
function process() {
    if grep --quiet debian /etc/os-release; then
        process_deb_packages
    elif grep --quiet rhel /etc/os-release; then
        process_rpm_packages
    else
        printf '%s\n' 'fatal: unknown os release' >&2
        exit 1
    fi
}


# This function downloads and installs Debian package kz.
function process_deb_packages() {
    local kz_deb_local_file=''
    local kz_deb_site_file=''
    local packages_url='https://karelzimmer.nl/assets/packages'
    local reinstall=''

    if $OPTION_DEV; then
        kz_deb_site_file=$packages_url/kz-dev_4.2.1-1_all.deb
        kz_deb_local_file=$(mktemp -t "kz-dev-XXXXXXXXXX.deb")
    else
        kz_deb_site_file=$packages_url/kz_4.2.1-1_all.deb
        kz_deb_local_file=$(mktemp -t "kz-XXXXXXXXXX.deb")
    fi

    if type kz &> /dev/null; then
        reinstall='--reinstall'
    fi

    # Download kz package.
    wget --output-document="$kz_deb_local_file" "$kz_deb_site_file"
    # Prevent the message ...'/tmp/...deb' couldn't be accessed by user '_apt'.
    chmod o+r "$kz_deb_local_file"

    # Refresh package lists before installing apps, to minimize the risk of
    # errors due to outdated package lists.
    sudo true || exit 1
    check_debian_package_manager
    sudo apt-get update
    printf '\n'

    # Install kz package.
    check_debian_package_manager
    yes | sudo apt-get install "$reinstall" "$kz_deb_local_file"

    # Install zenity if not already installed and DISPLAY is set (GUI).
    if ! type zenity > /dev/null && [[ -n ${DISPLAY-} ]]; then
        check_debian_package_manager
        sudo apt-get install --assume-yes zenity
    fi
}


# This function checks if a Debian package manager is already running and waits
# for the next check if so.
function check_debian_package_manager() {
    local text=''

    while sudo  fuser                           \
                --silent                        \
                /var/cache/debconf/config.dat   \
                /var/{lib/{dpkg,apt/lists},cache/apt/archives}/lock*; do
        sleep 2
    done
}


# This function installs RPM package kz.
function process_rpm_packages() {
    local install=''
    local kz_rpm_site_file=''
    local packages_url='https://karelzimmer.nl/assets/packages'

    if $OPTION_DEV; then
        kz_rpm_site_file=$packages_url/kz-dev-4.2.1-1.noarch.rpm
    else
        kz_rpm_site_file=$packages_url/kz-4.2.1-1.noarch.rpm
    fi

    if type kz > /dev/null; then
        install='reinstall'
    else
        install='install'
    fi

    # Refresh package lists before installing apps, to minimize the risk of
    # errors due to outdated package lists.
    sudo dnf check-update || true
    printf '\n'

    # Install kz package.
    sudo dnf "$install" --assumeyes "$kz_rpm_site_file"

    # Install zenity if not already installed and DISPLAY is set (GUI).
    if ! type zenity > /dev/null && [[ -n ${DISPLAY-} ]]; then
        sudo dnf install --assumeyes zenity
    fi
}


# This function controls the termination of the script.
function term() {
    local text=''
    local separator_line=''

    gpg --import /usr/share/kz/kz-public-key.gpg

    separator_line=$(eval printf '=%.0s' '{1..80}')
    text="\n$separator_line\n\n"

    if $OPTION_DEV; then
        text+="$RED$(gettext "Please note: the development version of \
the kz package is installed")$NORMAL\n\n"
    fi

    text+="$(gettext "Package kz has been successfully installed.

Now follow the steps as described in the Checklist installation.

Checklist installation can be found on https://karelzimmer.nl/en.")"
    if [[ -n ${DISPLAY-} ]]; then
        text+="

$(gettext "Type 'exit' to close this window.")"
    fi
    printf '%b\n' "$text"

    rm --force getkz* /tmp/kz-*.deb

    exit 0
}

# #############################################################################
# Main
# #############################################################################

init "$@"
process
term
