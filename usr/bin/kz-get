#!/usr/bin/env bash
# shellcheck shell=bash
# #############################################################################
# SPDX-FileComment: Download and install package kz.
#
# SPDX-FileCopyrightText: Karel Zimmer <info@karelzimmer.nl>
# SPDX-License-Identifier: CC0-1.0
# #############################################################################


# #############################################################################
# Imports
# #############################################################################

export TEXTDOMAIN='kz'
export TEXTDOMAINDIR='/usr/share/locale'
source /usr/bin/gettext.sh


# #############################################################################
# Constants
# #############################################################################

readonly PROGRAM_NAME='kz-get'
readonly PROGRAM_LOGS="systemd-cat --identifier=$PROGRAM_NAME"

# List NORMAL last here so that -x doesn't bork the display.
readonly BLUE='\033[1;34m'
readonly GREEN='\033[1;32m'
readonly RED='\033[1;31m'
readonly NORMAL='\033[0m'

# #############################################################################
# Globals
# #############################################################################

declare OPTION_DEV=false


# #############################################################################
# Functions
# #############################################################################

# This function performs initial actions.
init() {
    if [[ $* = '-d' || $*  = '--dev' ]]; then
        OPTION_DEV=true
    elif [[ -n $* ]]; then
        printf '%s\n' 'kz get [-d | --dev]'
        exit 0
    fi

    # Script-hardening.
    set -o errexit
    set -o errtrace
    set -o nounset
    set -o pipefail

    # Trap signals.
    trap 'rm --force getkz* /tmp/kz-*.deb' EXIT
    trap 'err_report' ERR SIGHUP SIGINT SIGTERM
}


# This function returns an error message.
errmsg() {
    logmsg "${RED}$*${NORMAL}"
    printf "${RED}%b${NORMAL}\n" "$*" >&2
}


# This function reports errors.
# shellcheck disable=SC2317,SC2329
err_report() {
            text+="
$(gettext "Use \"journalctl -xe\" to check what went wrong.")
$(gettext "The last few lines of the log are displayed here.")"
    printf "${RED}%s${NORMAL}\n" "$text" >&2
    journalctl  --all       \
                --catalog   \
                --lines     \
                --no-pager  \
                --pager-end \
                --identifier="$PROGRAM_NAME"
    text="$(eval_gettext "Program \$PROGRAM_NAME encountered an error.")"
    errmsg "$text"
}


# This function returns an informational message.
infomsg() {
    logmsg "$*"
    printf '%b\n' "$*"
}


# This function records a message to the log.
logmsg() {
    local grey='\033[90m'

    printf "${grey}%b${NORMAL}\n" "$*" |& $PROGRAM_LOGS
}


# This function performs the script actions.
process() {
    become_check
    sudo --validate || exit 1

    if grep --quiet --regexp='debian' /etc/os-release; then
        process_kz_deb_package
    elif grep --quiet --regexp='rhel\|fedora' /etc/os-release; then
        process_kz_rpm_package
    else
        errmsg 'fatal: unknown os release'
        exit 1
    fi
}

# This function returns 0 if the user is root or can become root, otherwise
# exits 0 with descriptive message.
become_check() {
    local text=''

    if [[ $UID -eq 0 ]]; then
        return 0
    elif groups "$USER" | grep --quiet --regexp='sudo' --regexp='wheel'; then
        return 0
    else
        text=$(gettext 'Already performed by the administrator.')
        infomsg "${GREEN}$text${NORMAL}"
        exit 0
    fi
}


# This function downloads and installs Debian package kz.
process_kz_deb_package() {
    local install='install'
    local kz_deb_local_file=''
    local kz_deb_site_file=''
    local packages_url='https://karelzimmer.nl/assets/packages'

    if $OPTION_DEV; then
        kz_deb_site_file=$packages_url/kz-dev_4.2.1-1_all.deb
        kz_deb_local_file=$(mktemp -t "kz-dev-XXXXXXXXXX.deb")
    else
        kz_deb_site_file=$packages_url/kz_4.2.1-1_all.deb
        kz_deb_local_file=$(mktemp -t "kz-XXXXXXXXXX.deb")
    fi

    if type kz &> /dev/null; then
        install='reinstall'
    fi

    # Refresh package lists before installing apps, to minimize the risk of
    # errors due to outdated package lists.
    printf '%s' 'Debian update.....'
    logmsg "Update package lists..."
    check_debian_package_manager
    sudo apt-get update |& $PROGRAM_LOGS

    # Download kz deb package.
    printf '\n%s' 'Deb kz download...'
    logmsg "Downloading $kz_deb_site_file..."
    wget    --output-document="$kz_deb_local_file"  \
            "$kz_deb_site_file"                     |& $PROGRAM_LOGS
    # Prevent the message ...'/tmp/...deb' couldn't be accessed by user '_apt'.
    chmod o+r "$kz_deb_local_file"

    # Skip user prompts during installations or upgrades.
    # This ensures an uninterrupted, automated installation process.
    export DEBIAN_FRONTEND=noninteractive

    # Install kz deb package.
    printf '\n%s' 'Deb kz install....'
    logmsg "Installing $kz_deb_site_file..."
    check_debian_package_manager
    sudo apt-get "$install" --assume-yes "$kz_deb_local_file" |& $PROGRAM_LOGS
}


# This function checks if a Debian package manager is already running and waits
# for the next check if so.
check_debian_package_manager() {
    local text='Wait for another package manager to finish...'

    while sudo  fuser                           \
                --silent                        \
                /var/cache/debconf/config.dat   \
                /var/{lib/{dpkg,apt/lists},cache/apt/archives}/lock*; do
        logmsg "$text"
        sleep 1
    done
}


# This function installs RPM package kz.
process_kz_rpm_package() {
    local install='install'
    local kz_rpm_site_file=''
    local packages_url='https://karelzimmer.nl/assets/packages'

    if $OPTION_DEV; then
        kz_rpm_site_file=$packages_url/kz-dev-4.2.1-1.noarch.rpm
    else
        kz_rpm_site_file=$packages_url/kz-4.2.1-1.noarch.rpm
    fi

    if type kz &> /dev/null; then
        install='reinstall'
    fi

    # Install kz rpm package.
    printf '%s' 'RPM kz install...'
    logmsg "Installing $kz_rpm_site_file..."
    sudo dnf "$install" --assumeyes "$kz_rpm_site_file" |& $PROGRAM_LOGS
}


# This function controls the termination of the script.
term() {
    local filler=''
    local line=''
    local text=''

    filler=$(printf ' %.0s' {1..7})
    line=$(printf '=%.0s' {1..79})

    text="\n$line\n"
    if $OPTION_DEV; then
        text+="\n$filler${RED}$(gettext "Please note: the development version \
of the kz package is installed")\
${NORMAL}\n"
    fi
    text+="\n$filler${GREEN}$(gettext "Package kz has been successfully \
installed.")${NORMAL}\n\n"
    text+="$filler$(gettext "Now follow the steps as described in the \
Checklist installation.")\n"
    text+="$filler$(gettext "Checklist installation can be found on \
https://karelzimmer.nl/en.")\n\n"
    if [[ -n ${XDG_CURRENT_DESKTOP-} ]]; then
        text+="$filler${BLUE}$(gettext "Type 'exit' to close this window.")\
${NORMAL}\n"
    fi
    text+="\n$line"
    infomsg "$text"

    exit 0
}


# #############################################################################
# Main
# #############################################################################

init "$@"
process
term
